<Page x:Class="TicketManagementSystem.Client.Views.TicketManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:TicketManagementSystem.Client.ViewModels"
      Title="Ticket Management">

    <Page.Resources>
        <!-- Ticket Border Style with Blue Color Range -->
        <Style x:Key="TicketBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E1E4E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Padding" Value="16,14,16,14"/>
            <Setter Property="MinHeight" Value="200"/>
            <Setter Property="Effect" Value="{StaticResource DropShadowEffect}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="To Do">
                    <Setter Property="BorderBrush" Value="#94A3B8"/>
                    <Setter Property="BorderThickness" Value="4,1,1,1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="In Progress">
                    <Setter Property="BorderBrush" Value="#3B82F6"/>
                    <Setter Property="BorderThickness" Value="4,1,1,1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Testing">
                    <Setter Property="BorderBrush" Value="#06B6D4"/>
                    <Setter Property="BorderThickness" Value="4,1,1,1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Resolved">
                    <Setter Property="BorderBrush" Value="#0EA5E9"/>
                    <Setter Property="BorderThickness" Value="4,1,1,1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Closed">
                    <Setter Property="BorderBrush" Value="#1E40AF"/>
                    <Setter Property="BorderThickness" Value="4,1,1,1"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>
    
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource GlobalHeaderStyle}" BorderBrush="#E0E0E0" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Title Row -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <Image Source="../Resources/PageLinkIcons/ticket.png"
                            Style="{StaticResource GlobalHeaderIconStyle}"/>
                    <TextBlock Text="Ticket Management" Style="{StaticResource GlobalHeaderTitleStyle}"/>
                </StackPanel>

                <!-- Search and Filter Row with Add Button -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Search and Filter Controls -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="Search:" Style="{StaticResource GlobalHeaderLabelStyle}" Width="60"/>
                        <TextBox x:Name="SearchBox" 
                                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                TextChanged="SearchBox_TextChanged"
                                Height="35"
                                Padding="8,6"
                                BorderThickness="1"
                                BorderBrush="#D1D5DB"
                                Background="White"
                                FontSize="14"
                                Width="200"
                                Margin="0,0,10,0"
                                VerticalAlignment="Center"/>
                        
                        <TextBlock Text="Assigned User:" Style="{StaticResource GlobalHeaderLabelStyle}" Width="90"/>
                        <ComboBox x:Name="AssignedUserFilter"
                                ItemsSource="{Binding AssignedUsers}" 
                                SelectedItem="{Binding SelectedAssignedUser}"
                                SelectionChanged="AssignedUserFilter_SelectionChanged"
                                Height="35"
                                Padding="8,6"
                                BorderThickness="1"
                                BorderBrush="#D1D5DB"
                                Background="White"
                                FontSize="14"
                                Width="150"
                                Margin="0,0,10,0"
                                VerticalAlignment="Center"/>

                        
                        <Button Content="Clear Filters" Command="{Binding ClearFiltersCommand}" 
                                Height="35"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Padding="12,0"
                                Background="{StaticResource PrimaryBrush}"
                                Foreground="White"
                                BorderThickness="0"
                                Cursor="Hand"
                                Width="100" 
                                Margin="10,0,0,0"
                                VerticalAlignment="Center">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="4"
                                                        BorderThickness="0"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2563EB"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                    
                    <!-- Add Button in Right Corner -->
                    <Button Grid.Column="1" Content="Add" Command="{Binding AddTicketCommand}" 
                            Height="35"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Padding="12,0"
                            Background="{StaticResource PrimaryBrush}"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Width="80" 
                            VerticalAlignment="Top">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    BorderThickness="0"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2563EB"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource ModernScrollBarStyle}"/>
            </ScrollViewer.Resources>
            <!-- Kanban Board -->
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                        
                <!-- Status Headers -->
                <Border Grid.Column="0" Background="{StaticResource CardBrush}" BorderBrush="#D1D5DB" BorderThickness="1" Margin="0,0,5,0" Padding="15,12,15,12" CornerRadius="4">
                    <TextBlock Text="To Do" FontWeight="SemiBold" FontSize="14" HorizontalAlignment="Center" Foreground="#2C3E50" TextOptions.TextFormattingMode="Display"/>
                </Border>
                <Border Grid.Column="1" Background="{StaticResource CardBrush}" BorderBrush="#D1D5DB" BorderThickness="1" Margin="5,0,5,0" Padding="15,12,15,12" CornerRadius="4">
                    <TextBlock Text="In Progress" FontWeight="SemiBold" FontSize="14" HorizontalAlignment="Center" Foreground="#2C3E50" TextOptions.TextFormattingMode="Display"/>
                </Border>
                <Border Grid.Column="2" Background="{StaticResource CardBrush}" BorderBrush="#D1D5DB" BorderThickness="1" Margin="5,0,5,0" Padding="15,12,15,12" CornerRadius="4">
                    <TextBlock Text="Testing" FontWeight="SemiBold" FontSize="14" HorizontalAlignment="Center" Foreground="#2C3E50" TextOptions.TextFormattingMode="Display"/>
                </Border>
                <Border Grid.Column="3" Background="{StaticResource CardBrush}" BorderBrush="#D1D5DB" BorderThickness="1" Margin="5,0,5,0" Padding="15,12,15,12" CornerRadius="4">
                    <TextBlock Text="Resolved" FontWeight="SemiBold" FontSize="14" HorizontalAlignment="Center" Foreground="#2C3E50" TextOptions.TextFormattingMode="Display"/>
                </Border>
                <Border Grid.Column="4" Background="{StaticResource CardBrush}" BorderBrush="#D1D5DB" BorderThickness="1" Margin="5,0,0,0" Padding="15,12,15,12" CornerRadius="4">
                    <TextBlock Text="Closed" FontWeight="SemiBold" FontSize="14" HorizontalAlignment="Center" Foreground="#2C3E50" TextOptions.TextFormattingMode="Display"/>
                </Border>
                
                <!-- Ticket Lists -->
                <ItemsControl Grid.Column="0" ItemsSource="{Binding TodoTickets}" AllowDrop="True" Drop="Todo_Drop" DragEnter="Todo_DragEnter" DragLeave="Todo_DragLeave" Margin="15,35,15,0" MinHeight="200" Background="Transparent">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource TicketBorderStyle}"
                                    MouseLeftButtonDown="Ticket_MouseLeftButtonDown" Tag="{Binding}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" Foreground="#1A202C" LineHeight="20" Margin="0,0,0,8"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" TextWrapping="Wrap" Foreground="#4A5568" LineHeight="18" Margin="0,0,0,10" MinHeight="100" MaxHeight="100" TextTrimming="WordEllipsis"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="📅" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding DueDate, StringFormat='yyyy-MM-dd'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/> <!-- Spacer -->
                                        <TextBlock Text="👤" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding AssignedUser.Username, FallbackValue='Unassigned'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/> <!-- Spacer -->
                                        <Button Content="✏️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="6" 
                                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                Click="EditTicket_Click" Tag="{Binding}" Margin="0,0,4,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#E3F2FD"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button Content="🗑️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="6" 
                                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                Click="DeleteTicket_Click" Tag="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#FFEBEE"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ItemsControl Grid.Column="1" ItemsSource="{Binding InProgressTickets}" AllowDrop="True" Drop="InProgress_Drop" DragEnter="InProgress_DragEnter" DragLeave="InProgress_DragLeave" Margin="15,35,15,0" MinHeight="200" Background="Transparent">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource TicketBorderStyle}"
                                    MouseLeftButtonDown="Ticket_MouseLeftButtonDown" Tag="{Binding}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" Foreground="#1A202C" LineHeight="20" Margin="0,0,0,8"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" TextWrapping="Wrap" Foreground="#4A5568" LineHeight="18" Margin="0,0,0,10" MinHeight="100" MaxHeight="100" TextTrimming="WordEllipsis"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="📅" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding DueDate, StringFormat='yyyy-MM-dd'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/> <!-- Spacer -->
                                        <TextBlock Text="👤" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding AssignedUser.Username, FallbackValue='Unassigned'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/> <!-- Spacer -->
                                        <Button Content="✏️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="6" 
                                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                Click="EditTicket_Click" Tag="{Binding}" Margin="0,0,4,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#E3F2FD"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button Content="🗑️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="6" 
                                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                Click="DeleteTicket_Click" Tag="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#FFEBEE"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ItemsControl Grid.Column="2" ItemsSource="{Binding ResolvedTickets}" AllowDrop="True" Drop="Resolved_Drop" DragEnter="Resolved_DragEnter" DragLeave="Resolved_DragLeave" Margin="15,35,15,0" MinHeight="200" Background="Transparent">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource TicketBorderStyle}"
                    MouseLeftButtonDown="Ticket_MouseLeftButtonDown" Tag="{Binding}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" Foreground="#1A202C" LineHeight="20" Margin="0,0,0,8"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" TextWrapping="Wrap" Foreground="#4A5568" LineHeight="18" Margin="0,0,0,10" MinHeight="100" MaxHeight="100" TextTrimming="WordEllipsis"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="📅" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding DueDate, StringFormat='yyyy-MM-dd'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/>
                                        <!-- Spacer -->
                                        <TextBlock Text="👤" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding AssignedUser.Username, FallbackValue='Unassigned'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/>
                                        <!-- Spacer -->
                                        <Button Content="✏️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="4" 
                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                Click="EditTicket_Click" Tag="{Binding}" Margin="0,0,4,0">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="2">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                        <Button Content="🗑️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="4" 
                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                Click="DeleteTicket_Click" Tag="{Binding}">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="2">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ItemsControl Grid.Column="3" ItemsSource="{Binding TestingTickets}" AllowDrop="True" Drop="Testing_Drop" DragEnter="Testing_DragEnter" DragLeave="Testing_DragLeave" Margin="15,35,15,0" MinHeight="200" Background="Transparent">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource TicketBorderStyle}"
                                    MouseLeftButtonDown="Ticket_MouseLeftButtonDown" Tag="{Binding}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" Foreground="#1A202C" LineHeight="20" Margin="0,0,0,8"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" TextWrapping="Wrap" Foreground="#4A5568" LineHeight="18" Margin="0,0,0,10" MinHeight="100" MaxHeight="100" TextTrimming="WordEllipsis"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="📅" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding DueDate, StringFormat='yyyy-MM-dd'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/>
                                        <!-- Spacer -->
                                        <TextBlock Text="👤" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding AssignedUser.Username, FallbackValue='Unassigned'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/>
                                        <!-- Spacer -->
                                        <Button Content="✏️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="6" 
                                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                Click="EditTicket_Click" Tag="{Binding}" Margin="0,0,4,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#E3F2FD"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button Content="🗑️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="6" 
                                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                Click="DeleteTicket_Click" Tag="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#FFEBEE"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ItemsControl Grid.Column="4" ItemsSource="{Binding ClosedTickets}" AllowDrop="True" Drop="Closed_Drop" DragEnter="Closed_DragEnter" DragLeave="Closed_DragLeave" Margin="15,35,15,0" MinHeight="200" Background="Transparent">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource TicketBorderStyle}"
                                    MouseLeftButtonDown="Ticket_MouseLeftButtonDown" Tag="{Binding}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" Foreground="#1A202C" LineHeight="20" Margin="0,0,0,8"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" TextWrapping="Wrap" Foreground="#4A5568" LineHeight="18" Margin="0,0,0,10" MinHeight="100" MaxHeight="100" TextTrimming="WordEllipsis"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="📅" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding DueDate, StringFormat='yyyy-MM-dd'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/> <!-- Spacer -->
                                        <TextBlock Text="👤" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding AssignedUser.Username, FallbackValue='Unassigned'}" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                                        <TextBlock Text="  " Width="20"/> <!-- Spacer -->
                                        <Button Content="✏️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="6" 
                                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                Click="EditTicket_Click" Tag="{Binding}" Margin="0,0,4,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#E3F2FD"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button Content="🗑️" FontSize="12" Background="Transparent" BorderThickness="0" Padding="6" 
                                                Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                Click="DeleteTicket_Click" Tag="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#FFEBEE"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
