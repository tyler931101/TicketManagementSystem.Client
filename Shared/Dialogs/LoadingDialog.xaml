<Window x:Class="TicketManagementSystem.Client.Shared.Dialogs.LoadingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading" 
        Height="180" 
        Width="350"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize">
    
    <Border Background="White"
            CornerRadius="12"
            BorderBrush="#E5E7EB"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" 
                    Background="#F9FAFB" 
                    CornerRadius="12,12,0,0" 
                    Padding="20,15"
                    MouseDown="TitleBar_MouseDown">
                <TextBlock Text="Loading" 
                           FontSize="16" 
                           FontWeight="SemiBold" 
                           Foreground="#111827"/>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Icon and Message -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock Text="⏳" FontSize="24" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="MessageText" 
                               Text="Loading..." 
                               FontSize="16" 
                               VerticalAlignment="Center" 
                               Foreground="#374151"/>
                </StackPanel>

                <!-- Progress Bar -->
                <ProgressBar Grid.Row="1" 
                             IsIndeterminate="True" 
                             Height="6" 
                             BorderThickness="0"
                             Foreground="{StaticResource PrimaryBrush}"
                             Background="#E5E7EB">
                    <ProgressBar.Clip>
                        <RectangleGeometry Rect="0,0,300,6" RadiusX="3" RadiusY="3"/>
                    </ProgressBar.Clip>
                </ProgressBar>
            </Grid>
        </Grid>
    </Border>
</Window>